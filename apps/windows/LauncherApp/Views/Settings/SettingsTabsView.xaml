<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LauncherApp.Views.Settings.SettingsTabsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tabs="using:LauncherApp.Views.Settings.Tabs"
    mc:Ignorable="d">

    <Grid Padding="6,0,6,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,4">
            <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                VerticalAlignment="Center"
                FontSize="15"
                FontWeight="SemiBold"
                Foreground="{StaticResource LauncherTextBrush}"
                Text="Settings" />

            <TextBlock
                Grid.Column="1"
                Margin="10,0,0,0"
                VerticalAlignment="Center"
                FontSize="11"
                Foreground="{StaticResource LauncherMutedTextBrush}"
                Text="Esc or Ctrl+Shift+, to close" />

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button MinWidth="110" Height="30" Content="Save Config" />
                <Button MinWidth="140" Height="30" Content="Back to Launcher" Click="BackToLauncherButton_OnClick" />
            </StackPanel>
            </Grid>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Padding="0" Background="Transparent" CornerRadius="0">
                <Grid ColumnSpacing="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button x:Name="AppearanceTabButton" Grid.Column="0" Height="34" Content="Appearance" Click="AppearanceTabButton_OnClick" />
                    <Button x:Name="AdvancedTabButton" Grid.Column="1" Height="34" Content="Advanced" Click="AdvancedTabButton_OnClick" />
                    <Button x:Name="ShortcutsTabButton" Grid.Column="2" Height="34" Content="Shortcuts" Click="ShortcutsTabButton_OnClick" />
                </Grid>
            </Border>

            <Border Grid.Row="1" Margin="0,4,0,0" Background="Transparent" CornerRadius="0" Padding="0">
                <Grid>
                    <tabs:AppearanceSettingsTabView x:Name="AppearanceTabContent" />
                    <tabs:AdvancedSettingsTabView x:Name="AdvancedTabContent" Visibility="Collapsed" />
                    <tabs:ShortcutsSettingsTabView x:Name="ShortcutsTabContent" Visibility="Collapsed" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
