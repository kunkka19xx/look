<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LauncherApp.Views.CommandPanels.CommandPanelsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Border
        Padding="12"
        Style="{StaticResource LauncherSurfaceBorderStyle}">
        <controls:TabView x:Name="CommandTabs">
            <controls:TabViewItem Header="calc">
                <StackPanel Padding="10" Spacing="8">
                    <TextBlock Foreground="{StaticResource LauncherTextBrush}" Text="Calculator command" />
                    <TextBlock Foreground="{StaticResource LauncherMutedTextBrush}" Text="Type expression after /calc" />
                </StackPanel>
            </controls:TabViewItem>
            <controls:TabViewItem Header="shell">
                <StackPanel Padding="10" Spacing="8">
                    <TextBlock Foreground="{StaticResource LauncherTextBrush}" Text="Shell command" />
                    <TextBlock Foreground="{StaticResource LauncherMutedTextBrush}" Text="Run shell with safety checks" />
                </StackPanel>
            </controls:TabViewItem>
            <controls:TabViewItem Header="kill">
                <StackPanel Padding="10" Spacing="8">
                    <TextBlock Foreground="{StaticResource LauncherTextBrush}" Text="Kill command" />
                    <TextBlock Foreground="{StaticResource LauncherMutedTextBrush}" Text="Requires confirm flow" />
                </StackPanel>
            </controls:TabViewItem>
            <controls:TabViewItem Header="sys">
                <StackPanel Padding="10" Spacing="8">
                    <TextBlock Foreground="{StaticResource LauncherTextBrush}" Text="System info command" />
                    <TextBlock Foreground="{StaticResource LauncherMutedTextBrush}" Text="Hardware/runtime summary panel" />
                </StackPanel>
            </controls:TabViewItem>
        </controls:TabView>
    </Border>
</UserControl>
