<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LauncherApp.Views.CommandPanels.CommandPanelsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" ColumnSpacing="4" RowSpacing="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="40" />
            </Grid.RowDefinitions>

            <ToggleButton x:Name="ShellCard" Grid.Row="0" Grid.Column="0" MinHeight="56" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Click="CommandCard_OnClick">
                <StackPanel Spacing="2">
                    <TextBlock FontWeight="SemiBold" Text="shell (Ctrl+1)" />
                </StackPanel>
            </ToggleButton>

            <ToggleButton x:Name="CalcCard" Grid.Row="0" Grid.Column="1" MinHeight="56" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Click="CommandCard_OnClick">
                <StackPanel Spacing="2">
                    <TextBlock FontWeight="SemiBold" Text="calc (Ctrl+2)" />
                </StackPanel>
            </ToggleButton>

            <ToggleButton x:Name="KillCard" Grid.Row="1" Grid.Column="0" MinHeight="56" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Click="CommandCard_OnClick">
                <StackPanel Spacing="2">
                    <TextBlock FontWeight="SemiBold" Text="kill (Ctrl+3)" />
                </StackPanel>
            </ToggleButton>

            <ToggleButton x:Name="SysCard" Grid.Row="1" Grid.Column="1" MinHeight="56" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Click="CommandCard_OnClick">
                <StackPanel Spacing="2">
                    <TextBlock FontWeight="SemiBold" Text="sys (Ctrl+4)" />
                </StackPanel>
            </ToggleButton>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource LauncherSurfaceBorderStyle}" Padding="12">
            <StackPanel Spacing="6">
                <TextBlock x:Name="CommandTitleText" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource LauncherTextBrush}" Text="calc" />
                <Border Background="#151D28" CornerRadius="8" Padding="8">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MinHeight="220" MaxHeight="320">
                        <TextBlock
                            x:Name="CommandOutputText"
                            FontFamily="Consolas"
                            Foreground="{StaticResource LauncherMutedTextBrush}"
                            Text=""
                            TextWrapping="Wrap" />
                    </ScrollViewer>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
